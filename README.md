# Only Bingo!

A simple bingo board creator with shareable links.

## Setup

```bash
bun install
```

Create a `.env` file:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/onlybingo
```

## Development

```bash
bun run dev
```

## Database

```bash
bun run db:push
```
